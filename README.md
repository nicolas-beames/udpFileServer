# udpFileServer
A file server using the UDP protocol

To run the server:
    >python server.py

To run the client:
with GUI:
    >activate venv
    >python PyQt/client.py
without
    >python Client/client.py